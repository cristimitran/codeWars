let text = "aabbcde"

let lowercase = text.toLowerCase()
    let length = lowercase.length //?
    let array = lowercase.split('') //?
    let count = 0
  

function duplicateCount(text){
    let k = 0
    let temparray = array.slice(1)//?
    for(i=0;i<length;i++){
      if(array[i]/*?*/ === temparray[i]/*?*/) {

      }
        
    }
  }

duplicateCount(text)

/* 
str.toLowerCase()
*/